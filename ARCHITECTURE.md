# Architecture

- `lib/engine.py` owns the FluidAudio subprocess contract and JSON normalization.
- `lib/run.py` owns the foreground run and `transcript.md`, `transcript.json`, and `manifest.json` artifacts.
